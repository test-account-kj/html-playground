# Notes

These notes capture the in-progress thinking for the next iteration of
the playground site:

- We want a Notes section linked from the landing page.
- Eventually the notes should be sourced from a markdown file rendered
  at build time rather than written inline.
